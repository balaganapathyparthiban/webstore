const SearchBar: React.FC = () => {
  return (
    <div className="w-full h-full">
      <input type="text" className="w-full h-full px-4 border rounded shadow" placeholder="Search for apps & games" />
    </div>
  );
};

export default SearchBar;
